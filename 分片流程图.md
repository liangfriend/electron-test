@startuml
start
:提供服务器文件数据区间，向远程服务器请求数据;
:将每次服务器返回的二进制数据以字符串形式拼接;
if(是否中途出错) then (没出错)
    :递增区间起始值;
else (出错)
    :保存出错前最近一次区间起始值;
endif
:将缓存的起始值传入服务器再次请求数据;
:服务器提示客户端数据传输完成;
:客户端将拼接的字符串数据写入文件，存入本地;
stop
@enduml